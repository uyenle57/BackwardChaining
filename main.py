# -*- coding: utf-8 -*-#import textwrapfrom BackwardChaining.InferenceEngine import InferenceEnginefrom BackwardChaining.KnowledgeBase import KnowledgeBasefrom BackwardChaining.WorkingMemory import WorkingMemorydef main():    print(textwrap.dedent("""    ================================================================                      ARTIFICIAL INTELLIGENCE            Coursework 2: Backward Chaining Rule Based System                                Uyen Le                        tle004@gold.ac.uk    ================================================================    \n"""))    # Create the Knowledge Base    knowledge_base = KnowledgeBase()    # Define the Working Memory    working_memory = WorkingMemory([('f', 'h'), ('d', 'j'), ('u', 'v'), ('r', 't')])    # Define the goal    goal = ('b', 'g')    print("\nInference engine parameters:")    print("\tWorking Memory: ", working_memory.get_facts())    print("\tGoal: ", goal)    print("\nStarting inference engine...")    # Inference Engine    inference_engine = InferenceEngine(working_memory, knowledge_base)    inference_engine.backward_chaining(goal)main()